apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orders-service
  template:
    metadata:
      labels:
        app: orders-service
    spec:
      containers:
      - name: orders-service
        image: [YOUR_ECR_CATALOG_IMAGE]
        ports:
        - containerPort: 3000
        env:
        - name: DB_HOST
          value: [YOUR-RDS-ENDPOINT]
        - name: DB_USER
          value: postgres
        - name: DB_NAME
          value: postgres
        - name: DB_PASS
          value: testing
        - name: DB_PORT
          value: "5432"
---
apiVersion: v1
kind: Service
metadata:
  name: orders-service
spec:
  type: LoadBalancer
  selector:
    app: orders-service
  ports:
  - port: 3000
    targetPort: 3000
